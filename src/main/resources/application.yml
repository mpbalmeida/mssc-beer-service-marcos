sfg:
  brewery:
    beer-inventory-service-host: http://localhost:8082
spring:
  artemis:
    user: artemis
    password: simetraehcapa
  datasource:
    initialization-mode: embedded
    url: jdbc:h2:mem:testdb;MODE=MYSQL
  cache:
    jcache:
      config: classpath:ehcache.xml
logging:
  level:
    dev.marcosalmeida: debug
